<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <!-- GL view fills the entire screen; all overlays sit on top -->
    <com.overdrive.app.ui.view.MultiCameraGLView
        android:id="@+id/glView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!--
        2×2 grid overlay — mirrors the GL quad positions exactly.
        Each cell covers one quadrant and is tappable to enter fullscreen for that camera.
        Shown in GRID mode; hidden in FULLSCREEN mode.
        The thin black divider is the GL clear colour showing through the g=0.015 gap.
    -->
    <LinearLayout
        android:id="@+id/gridOverlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- Top row: FRONT (left) | RIGHT (right) -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:orientation="horizontal">

            <FrameLayout
                android:id="@+id/gridCellFront"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:foreground="?attr/selectableItemBackground">

                <TextView
                    android:id="@+id/tvLabelFront"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="bottom|start"
                    android:layout_margin="6dp"
                    android:paddingHorizontal="6dp"
                    android:paddingVertical="2dp"
                    android:background="#66000000"
                    android:textColor="#CCFFFFFF"
                    android:textSize="10sp"
                    android:fontFamily="sans-serif-medium"
                    android:text="FRONT" />
            </FrameLayout>

            <FrameLayout
                android:id="@+id/gridCellRight"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:foreground="?attr/selectableItemBackground">

                <TextView
                    android:id="@+id/tvLabelRight"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="bottom|start"
                    android:layout_margin="6dp"
                    android:paddingHorizontal="6dp"
                    android:paddingVertical="2dp"
                    android:background="#66000000"
                    android:textColor="#CCFFFFFF"
                    android:textSize="10sp"
                    android:fontFamily="sans-serif-medium"
                    android:text="RIGHT" />
            </FrameLayout>
        </LinearLayout>

        <!-- Bottom row: REAR (left) | LEFT (right) -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:orientation="horizontal">

            <FrameLayout
                android:id="@+id/gridCellRear"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:foreground="?attr/selectableItemBackground">

                <TextView
                    android:id="@+id/tvLabelRear"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="bottom|start"
                    android:layout_margin="6dp"
                    android:paddingHorizontal="6dp"
                    android:paddingVertical="2dp"
                    android:background="#66000000"
                    android:textColor="#CCFFFFFF"
                    android:textSize="10sp"
                    android:fontFamily="sans-serif-medium"
                    android:text="REAR" />
            </FrameLayout>

            <FrameLayout
                android:id="@+id/gridCellLeft"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:foreground="?attr/selectableItemBackground">

                <TextView
                    android:id="@+id/tvLabelLeft"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="bottom|start"
                    android:layout_margin="6dp"
                    android:paddingHorizontal="6dp"
                    android:paddingVertical="2dp"
                    android:background="#66000000"
                    android:textColor="#CCFFFFFF"
                    android:textSize="10sp"
                    android:fontFamily="sans-serif-medium"
                    android:text="LEFT" />
            </FrameLayout>
        </LinearLayout>
    </LinearLayout>

    <!--
        Top bar: back + title + meta + grid-return button.
        Starts GONE. Shown on tap in FULLSCREEN mode; hidden after 3s of playback.
        btnGridView is only visible in FULLSCREEN mode.
    -->
    <LinearLayout
        android:id="@+id/topBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="12dp"
        android:background="#80000000"
        android:layout_gravity="top"
        android:visibility="gone">

        <ImageButton
            android:id="@+id/btnBack"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_back"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Back"
            android:tint="#FFFFFF"
            android:scaleType="centerInside" />

        <TextView
            android:id="@+id/tvTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:fontFamily="sans-serif-medium"
            android:maxLines="1"
            android:ellipsize="middle" />

        <TextView
            android:id="@+id/tvMeta"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#B0FFFFFF"
            android:textSize="12sp"
            android:layout_marginEnd="8dp" />

        <!-- Shown only in FULLSCREEN mode to return to the 2×2 grid -->
        <ImageButton
            android:id="@+id/btnGridView"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_grid"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Grid view"
            android:tint="#FFFFFF"
            android:scaleType="centerInside"
            android:visibility="gone" />
    </LinearLayout>

    <!--
        Bottom controls: seekbar + play/pause + time display.
        Starts GONE. Shown on tap in FULLSCREEN mode; hidden after 3s of playback.
        Event timeline removed — it was visually crowded and redundant in this view.
    -->
    <LinearLayout
        android:id="@+id/bottomControls"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_gravity="bottom"
        android:background="#CC000000"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        android:paddingTop="4dp"
        android:paddingBottom="8dp"
        android:visibility="gone">

        <SeekBar
            android:id="@+id/seekBar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:progressTint="#6C63FF"
            android:thumbTint="#6C63FF"
            android:progressBackgroundTint="#55FFFFFF" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingStart="4dp"
            android:paddingEnd="4dp">

            <ImageButton
                android:id="@+id/btnPlayPause"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:src="@android:drawable/ic_media_pause"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="Play/Pause"
                android:tint="#FFFFFF"
                android:scaleType="centerInside" />

            <TextView
                android:id="@+id/tvCurrentTime"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="0:00"
                android:textColor="#FFFFFF"
                android:textSize="13sp"
                android:fontFamily="monospace"
                android:layout_marginStart="8dp" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text=" / "
                android:textColor="#80FFFFFF"
                android:textSize="13sp" />

            <TextView
                android:id="@+id/tvDuration"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="0:00"
                android:textColor="#FFFFFF"
                android:textSize="13sp"
                android:fontFamily="monospace" />

        </LinearLayout>
    </LinearLayout>

</FrameLayout>
